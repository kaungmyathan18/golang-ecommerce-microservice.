# Copy to .env and adjust. Values here override compiled defaults; real environment variables override .env.

APP_ENV=development

# HTTP: listen port is defined in config.DefaultConfig (API default 8080). Wire viper + envInt("SERVER_PORT", 8080) there if you want PORT from the environment.

# Logging
LOG_LEVEL=info
LOG_FORMAT=console

# OpenTelemetry (matches observability preset that enables OTLP)
OTEL_ENABLED=true
OTEL_SERVICE_NAME=
OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317
OTEL_INSECURE=true
# postgresql://postgres:admin@127.0.0.1/chat_service?statusColor=686B6F&env=local&name=chat-service&tLSMode=0&usePrivateKey=false&safeModeLevel=0&advancedSafeModeLevel=0&driverVersion=0&lazyload=false
# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=ecommerce-products
DB_SSLMODE=disable
DB_AUTO_MIGRATE=false

# Redis — used for cache and queue (same instance by default).
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Continuous profiling (only when observability docker stack / Pyroscope is enabled)
PYROSCOPE_SERVER_ADDRESS=http://localhost:4040
# With compose: Prometheus http://localhost:9090, Grafana http://localhost:3000, Pyroscope http://localhost:4040
