services:
  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      APP_ENV: development
      DB_DSN: file:/data/app.db?_foreign_keys=on
    volumes:
      - sqlite-data:/data
volumes:
  sqlite-data:
