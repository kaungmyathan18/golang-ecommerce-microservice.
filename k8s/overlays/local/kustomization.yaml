apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

patchesStrategicMerge:
  - patches/image-pull-policy.yaml
  - patches/gateway-nodeport.yaml
